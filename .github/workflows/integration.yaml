name: "Integration workflow"
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build_package:
    name: Build package
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the branch
        uses: actions/checkout@v2
      - name: Setup node 12.x
        uses: actions/setup-node@v1
        with:
          node-version: '12.x'
          registry-url: 'https://registry.npmjs.org'
      - name: Get yarn cache directory path
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn cache dir)"
      - uses: actions/cache@v1
        id: yarn-cache # use this to check for `cache-hit` (`steps.yarn-cache.outputs.cache-hit != 'true'`)
        with:
          path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-
      - name: Install dependencies
        run: yarn install --frozen-lockfile
      - name: Build the package
        run: yarn run pack
      - name: Resolve package file path
        id: resolve-package-file
        run: echo "::set-output name=path::$(find . -name *.tgz)"
      - name: Upload the package
        uses: actions/upload-artifact@v1
        with:
          name: package
          path: ${{ steps.resolve-package-file.outputs.path }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPMJS_ORG_READER_TOKEN }}

  test_package:
    name: Test package
    runs-on: ubuntu-latest
    needs:
      - build_package
    steps:
      - name: Setup node 12.x
        uses: actions/setup-node@v1
        with:
          node-version: '12.x'
          registry-url: 'https://registry.npmjs.org'
      - name: Get yarn cache directory path
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn cache dir)"
      - uses: actions/cache@v1
        id: yarn-cache # use this to check for `cache-hit` (`steps.yarn-cache.outputs.cache-hit != 'true'`)
        with:
          path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-
      - name: Install dependencies
        run: yarn install --frozen-lockfile

  prepare_patch:
    name: Prepare a patch
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source branch
        uses: actions/checkout@v2
        with:
          ref: releases/v1.0
          fetch-depth: 0
      - name: Configure merge driver(s)
        run: git config merge.versioned-json.driver $(realpath scripts/merge_versioned_json.sh)
      - name: Merge changes
        run: git fetch && git merge origin/master
      - name: Push merged
        run: git add . && git commit -m"Merge master" && git push origin ${GITHUB_REF#"refs/heads/"}:patches/v1.0/master